Deploy instructions:
1) Upload/overwrite index.html at the REPO ROOT (same level as /docs and /pricing folders).
2) Upload .nojekyll at the REPO ROOT.
3) If finternetops.moecommunitycloud.com still shows the MBCC main page at '/', delete/rename any index.md or redirect rules (_redirects) in this repo.
4) Hard refresh (Ctrl+F5).
